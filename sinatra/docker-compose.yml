version: '2'
services:
  db:
    image: postgres
  web:
    build: .
    command:  bundle exec ruby app.rb -e production
    volumes:
      - .:/myapp
    ports:
      - "4567:4567"
    depends_on:
      - db

